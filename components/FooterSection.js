// components/FooterSection.js
export function initFooterSection() {
  // Footer HTML is already in index.html (static).
  // This module is kept for future dynamic footer features.
  const footer = document.getElementById('footer');
  if (!footer) return;
  // No-op: content already rendered in HTML
}
